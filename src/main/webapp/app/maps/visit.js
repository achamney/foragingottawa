var hasMapLoaded = false;
define([],
    function () {
        var component = ng.core.Component({
            selector: 'visit',
            templateUrl: 'app/maps/visit.html'
        }).Class({

            constructor: function () {
            }
        });
        return component;
});